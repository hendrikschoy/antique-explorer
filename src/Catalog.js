import { useEffect } from "react";

const Catalog = (props) => {

    useEffect(() => {
        // fetch catalog items
    }, [])

    return(
        <div>
        </div>
    )
}

export default Catalog;